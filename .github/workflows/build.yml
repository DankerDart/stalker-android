name: Android CI Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Look At Github Files
      run: |
        echo "=== СМОТРИМ ЧТО ЛЕЖИТ В РЕПОЗИТОРИИ ==="
        ls -la
